query GetUserTeams {
    getTeams {
        ...TeamFields
    }
}

mutation CreateTeam($input: CreateTeamInput!) {
    createTeam(input: $input) {
        ...TeamFields
    }
}

fragment TeamFields on Team {
    id
    tid
    name
}