query UserProfile($id: ID!) {
    getUserProfile(id: $id) {
        ...UserFields
    }
}

fragment UserFields on User {
    id
    uid
    name
    avatar {
        url
        filename
    }
}