query GetTeamByTid($tid: String!) {
    getTeam(tid: $tid) {
        id
        tid
        name
        myPermissions {
            permissionLevel
        }
        members {
            profile {
                uid
                name
                avatar {
                    url
                }
            }
            permission {
                permissionLevel
            }
        }
    }
}

query SearchProfiles($input: SearchProfilesInput!) {
    searchProfiles(input: $input) {
        profiles {
            id
            name
            avatar {
                url
            }
        }
        count
    }
}