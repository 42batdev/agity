query GetTeamMeetings($input: GetTeamInput!) {
    getTeam(input: $input) {
        id
        uid
        tid
        meetings {
            id
            name
            state
        }
    }
}

mutation CreateMeeting($input: CreateMeetingInput!) {
    createMeeting(input: $input) {
        id
    }
}